
spring:
  batch:
    #    job:
    #      enabled: false
    initialize-schema: always
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring_batch
    username: 'jobin'
    password: 'turbodriveA1@'
    initialization-mode: always
    continue-on-error: true
    platform: mysql